<!DOCTYPE html>
<html lang="en">
{{> Head }}
<body>
{{> Titlebar }}

<div class="ui main text container">
    <h1 class="ui header">{{subject.name}}'s wall</h1>

    <div class="ui list divided">
        {{#socialEvents}}
            <div class="item">
                <img class="ui avatar image" src="/assets/images/ic_note_black_24px.svg">
                <div class="content">
                    <a class="header" href=/wall/{{author.name}}>{{author.name}}</a>
                    <div class="description">{{content}}</div>
                </div>
            </div>
        {{/socialEvents}}
    </div>

    <form name="form" action="/wall/{{subject.name}}/write" method="post">
        <div class="ui action input">
            <input name="message" type="text" placeholder="Hey, what's up?"/>
            <input class="ui button" type="submit" value="Post"/>
        </div>
    </form>

</div>

</body>
</html>